using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ResultScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
		PlayerPrefs.GetInt ("dum_score", 0);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
